<!--$("#<%= @taskName %>_InputBox").on("keypress", function(ev) {-->
<!--	var keycode = (ev.keyCode ? ev.keyCode : ev.which);-->
<!--	if (keycode == '13') {-->
<!--		var statusJSON = {-->
<!--			"commandSet": "chat",-->
<!--			"chatCommand": "sendMessage",-->
<!--			"chatTarget": $(this).attr("chatRoom"),-->
<!--			"sendMessage": {-->
<!--				"msg": $(this).val(),-->
<!--			},-->
<!--		};-->
<!--		wsSendMsg(JSON.stringify(statusJSON));-->
<!--		console.log("Sending message to " + $(this).attr("chatRoom"));-->
<!--		$(this).val('');-->
<!--	}-->
<!--});-->

    refreshSortable();
    initializeEditable();
    initializeTaskContextMenu();
    var thisTableId = "<%= @taskName %>_TaskSpace";
    var cells = $('#' + thisTableId).find('th');
    cells.eq(0).width(120);
    console.log(cells.eq(0));
    fixTaskWidth(thisTableId);
    cells.eq(0).width("auto");
    taskTableInfo[thisTableId] = {taskColumnCount: 2, taskRowCount: 1, taskCellCount: 2, taskCount: 0, taskMoveInProgress: false, taskRowMoveInProgress: false, taskColumnMoveInProgress: false};


