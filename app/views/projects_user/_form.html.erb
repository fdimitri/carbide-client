<%= form_for @projects_user, :url => { :action => :create }, html: { method: :post } do |f| %>

<!--%= form_for(@projects_user) do |f| %> !-->
  <% if @projects_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@projects_user.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @projects_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :User_id %><br>
    <%= f.text_field :User_id %>
  </div>
  <div class="field">
    <%= f.label :Project_id %><br>
    <%= f.text_field :Project_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
